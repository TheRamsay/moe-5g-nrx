# @package _global_
# Experiment 04: Dense Capacity Sweep - Mid

defaults:
  - override /model: static_dense
  - override /dataset: mixed

experiment:
  batch_name: dense-capacity-v1
  exp_name: dense_mid_s56_b8_h48_bs32_lr1e3_s67
  tags: [baseline, dense, capacity, synthetic, simo-1x4, 16qam]
  notes: "Mid-capacity dense baseline for the capacity sweep (~306k parameters), trained on mixed UMa/TDL-C batches."
  study_slug: 2026-03-29-dense-capacity-v1
  study_path: experiments/2026-03-29-dense-capacity-v1
  question: What dense baseline capacity is strong enough without being unnecessarily large?

model:
  backbone:
    stem_hidden_dims: [64, 64]
    block_hidden_dim: 48
    readout_hidden_dim: 128

training:
  max_steps: 10000
