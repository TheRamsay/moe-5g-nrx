# @package _global_
# Experiment 01: Baseline Dense Model
# Reference point for comparison

defaults:
  - override /model: static_dense

training:
  max_steps: 10000
  batch_size: 32
  learning_rate: 0.001

logging:
  use_wandb: true

# Tags for wandb
tags: [baseline, dense, reference]
