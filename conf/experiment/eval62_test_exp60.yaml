# @package _global_
# Eval62: locked test-set evaluation of exp60 (100k + α=1e-3).
# Locks the headline number for the α/data scaling finding in the report.

defaults:
  - override /model: moe_nl
  - override /dataset: mixed

experiment:
  batch_name: alpha-data-test-eval-v1
  exp_name: eval_exp60_test
  tags: [eval, moe, alpha-data, test-set, asym-warm, hf, simo-1x4, 16qam]
  notes: >
    Test-set eval of exp60 (100k samples × α=1e-3, asym warm start, seed 67)
    on the locked dense-v1 test set. Locks headline number for the α/data
    scaling principle.
  study_slug: 2026-05-01-alpha-data-test-eval-v1
  study_path: experiments/2026-05-01-alpha-data-test-eval-v1
  question: Does exp60's val-set BLER ~0.902 hold on the test set?

evaluation:
  profiles: [uma, tdlc]
  snr_bins: 7
  use_artifacts: false
